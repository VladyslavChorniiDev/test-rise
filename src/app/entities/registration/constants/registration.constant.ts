import { IBreadcrumb } from "@shared/title/interfaces/breadcrumb.interface";

export const REGISTRATION_BREADCRUMBS: IBreadcrumb[] = [{ name: 'Home', path: '/' }, { name: 'Register' }];

export const DEFAULT_CODE: string = '777777';